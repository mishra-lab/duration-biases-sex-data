\begin{tikzpicture}[x=1em,y=3em,arrow/.append style={->}]
  \rvs[fix]{N}   {+5,4}{$N$}{0}
  \rvs[fix]{urm} {+1,5}{U}{0}
  \rvs[gen]{rm}  {+1,4}{$\bar{r}$}{0}
  \rvs[obs]{phi} {+3,3}{$\phi$}{0}
  \rvs[med]{ri}  {-1,3}{$r_i$}{\Ni}
  \rvs[fix]{ufi} {-7,4}{U\hskip1pt*}{0}
  \rvs[med]{fi}  {-7,3}{$f_i$}{\Ni}
  \rvs[fix]{uDm} {-4,5}{U}{0}
  \rvs[aim]{Dm}  {-4,4}{$\bar{D}$}{0}
  \rvs[med]{Di}  {-4,3}{$D_i$}{\Ni}
  \rvs[med]{di}  {-4,2}{$d_i$}{\Ni}
  \rvs[fix]{dz}  { 0,2}{$d_z$}{\Nz}
  \rvs[gen]{rhom}{-2,1}{$\bar{\rho}_z$}{\Nz}
  \rvs[fix]{Nz}  {+2,1}{$N_z$}{\Nz}
  \rvs[obs]{rhoz}{ 0,0}{$\rho_z$}{\Nz}
  \draw[arrow] (N)    -- (phi);
  \draw[arrow] (urm)  -- (rm);
  \draw[arrow] (rm)   -- (phi);
  \draw[arrow] (rm)   -- (ri);
  \draw[arrow] (uDm)  -- (Dm);
  \draw[arrow] (Dm)   -- (Di);
  \draw[arrow] (Di)   -- (di);
  \draw[arrow] (ufi)  -- (fi);
  \draw[arrow] (fi)   -- (di);
  \draw[arrow] (ri)   -- (di);
  \draw[arrow] (di)   -- (rhom);
  \draw[arrow] (dz)   -- (rhom);
  \draw[arrow] (Nz)   -- (rhoz);
  \draw[arrow] (rhom) -- (rhoz);
\end{tikzpicture}